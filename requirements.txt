# User Activity Analyzer - Requirements

# Python standard library only - no external dependencies required
# This project uses only Python standard library modules:
# - sqlite3 (built-in)
# - datetime (built-in)
# - typing (built-in)

# For development and testing (optional):
pytest>=7.0.0
pytest-cov>=4.0.0
